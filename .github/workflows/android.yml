name: Android CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    # KUNCI PERBAIKAN: Gunakan Gradle langsung dari mesin GitHub
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    # Bangun menggunakan command 'gradle' murni, bukan './gradlew'
    - name: Build with Gradle
      run: gradle assembleDebug --stacktrace

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: simas-at-taqwa-apk
        path: app/build/outputs/apk/debug/app-debug.apk
